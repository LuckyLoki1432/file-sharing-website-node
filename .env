# MongoDB URI (example, replace with your actual MongoDB URI)
MONGODB_URI=mongodb://localhost:27017/file-sharing-app

# Session secret (example, replace with a randomly generated secret)
SESSION_SECRET=your_session_secret_here

# Port for the server to listen on (optional, defaults to 3000 if not specified)
PORT=3000
